name: DevSecOps Spring Boot Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-and-security-scan:
    name: Build Java & Security Scan
    runs-on: ubuntu-latest

    steps:
      - name: 1. Checkout Code
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98 # v3.6.0
        with:
          fetch-depth: 0

      - name: 2. Set up JDK 26
        uses: actions/setup-java@0ab4596768b603586c0de567f2430c30f5b0d2b0 # v3.13.0
        with:
          java-version: "26"
          distribution: "temurin"
          cache: maven

      - name: 3. Build with Maven
        run: ./mvnw clean package -DskipTests

      - name: 4. SAST Scan (Semgrep)
        run: |
          docker run --rm -v $(pwd):/src returntocorp/semgrep semgrep --config=auto /src

      - name: 5. SCA Dependency Scan (Trivy)
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1 # v0.35.0
        with:
          scan-type: "fs"
          scan-ref: "."
          severity: "HIGH,CRITICAL"
          exit-code: "0"

      # ───【 第二階段新新增：容器安全 】───
      # 6. 打包 Docker Image
      - name: 6. Build Docker Image
        run: |
          docker build -t spring-boot-demo-app:${{ github.sha }} .

      # 7. 容器鏡像掃描 (Trivy for Docker Image)
      - name: 7. Container Image Scan (Trivy)
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1 # v0.35.0
        with:
          scan-type: "image"
          image-ref: "spring-boot-demo-app:${{ github.sha }}"
          severity: "HIGH,CRITICAL"
          exit-code: "0"
